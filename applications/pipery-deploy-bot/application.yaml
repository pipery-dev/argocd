apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pipery-deploy-bot
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/pipery-dev/pipery-deploy-bot.git
      targetRevision: main
      path: charts/pipery-deploy-bot
      helm:
        valueFiles:
          - values.yaml
          - $values/applications/pipery-deploy-bot/values.yaml
    - repoURL: https://github.com/pipery-dev/pipery-argocd.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: pipery
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
